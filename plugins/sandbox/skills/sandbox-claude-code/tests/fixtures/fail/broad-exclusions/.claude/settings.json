{
  "sandbox": {
    "enabled": true,
    "failIfUnavailable": true,
    "excludedCommands": ["docker *", "bash *"],
    "network": {
      "allowedDomains": ["registry.npmjs.org"]
    }
  },
  "permissions": {
    "deny": ["Read(./.env)", "Read(~/.ssh/**)", "Read(~/.aws/**)"]
  }
}
