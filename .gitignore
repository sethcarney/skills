# This repo's own local Claude Code config, and nothing else. Anchored to the
# root on purpose: an unanchored `.claude` also matches the nested
# `.claude/settings.json` files the sandbox test fixtures are built from, and
# silently drops them from every commit.
.claude/*
!.claude/settings.json
